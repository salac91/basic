<%= form_for [ @user, @article, @comment ] do |f| %>

  <div class="form-group">
    <%= f.text_area :body, class: 'form-control' %>
  </div>

   <%= f.submit "Comment", class: 'btn btn-primary' %>

<% end %>